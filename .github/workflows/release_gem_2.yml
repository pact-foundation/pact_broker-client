name: Release gem

on:
  repository_dispatch:
    types:
      - release-patch-2

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - uses: ./
